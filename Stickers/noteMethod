Итак : отличие вызова метода от декларации.
Декларация метода это вид типа
(модификатор доступа,
void(если нет возращаемого значения)
тип возвращаемого значения (если есть)
имя метода
параметры в скобках или пустые скобки

Вызов метода это только имя метода со скобками (пустыми или с передаваемыми аргементами) за точной (оператором доступа)


Определение(definition) метода это тело метода, определяем что именно метод делает. То есть это реализация метода.
